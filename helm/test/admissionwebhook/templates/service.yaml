---
# Source: admissionwebhook/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: RELEASE-NAME-admissionwebhook
  labels:
    app: admissionwebhook
    chart: admissionwebhook-0.1.0
    release: RELEASE-NAME
    heritage: Tiller
  namespace: cluster-services
spec:
  type: ClusterIP
  ports:
    - port: 443
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: admissionwebhook
    release: RELEASE-NAME
